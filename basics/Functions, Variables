"""
#Ask user for their name
name = input ("What's your name?")

#here as I have used a ',' i dont need to give any space after hello it'll automatically take one
print("Hello," , name)

if I don't wanna take a ',' instead I can use simply a '+' sign but i I do so i have to add a spacebar in the double quotes as it does not take one automatically

print("Hello, " + name)
"""



#SOMETHING REALLY IMPORTANT : DOCUMENTATION
"""
print(*objects, sep=' ' , end=\n)
"""

#USING end
"""
print("Hello,", end="")
print(name)

this simply means that after 'Hello,' the next line thing is gonna continue...
If we do it as
print("Hello,", end="???")
print(name)
the output screen will have '???' and then the input by the user
"""

#USING sep
"""
print("Hello,", sep=" ")
print(name)

this will simply give a spacebar between like it will seperate them as we have given the space between the double quotes in sep parameter
If we do it as
print("Hello,", sep="???")
print(name)
the output screen will seperate 'Hello and the input' by '???' as I have wrote here
"""

#end & sep are called NAMED PARAMETERS



#Most appropriate way to solve this problem is

name = input("What's your name?")
print(f"Hello, {name}")
#as soon as I add the 'f' just before the double quotes it treats the string as a special string
